# 📊 گام ۳: ساخت دیتاست آموزشی

## 🎯 هدف

در این گام، از داده‌های کرول‌شده در گام قبل، سه نوع دیتاست آموزشی در فرمت **Alpaca** ساخته شد.

این دیتاست‌ها برای **Fine-tuning مدل زبانی** استفاده می‌شوند و هرکدام جنبه متفاوتی از دانش کدنویسی را پوشش می‌دهند:

- آموزش تولید کد (Instruction)
- سوال و جواب فنی (QA)
- دیباگ و Code Review (Debug)

---

## 📁 فرمت دیتاست

تمام دیتاست‌ها در فرمت **Alpaca** و به‌صورت `JSONL` تولید شدند.

هر خط شامل یک نمونه آموزشی با فیلدهای زیر است:

- `instruction` → دستورالعمل یا سوال  
- `input` → کانتکست یا اطلاعات تکمیلی  
- `output` → پاسخ مورد انتظار مدل  

علاوه بر این سه فیلد، هر نمونه دارای متادیتای زیر نیز هست:

- `source` → منبع داده (code / issue / pr / commit / readme)
- `type` → نوع نمونه آموزشی

---

# 🧠 دیتاست اول: Instruction (آموزش کدنویسی)

این دیتاست از سه منبع ساخته شد:

---

## 1️⃣ استخراج از سورس کد

از فایل‌های `.py` توابع و کلاس‌های پایتون استخراج شدند.

مراحل پردازش:

- پردازش خط‌به‌خط فایل
- شناسایی `def` و `class`
- استخراج کامل بدنه

### ساخت نمونه آموزشی

- اگر تابع **docstring** داشت:
  - `instruction` از روی docstring ساخته شد  
  - مثال:  
    > Write a Python function named `get_user` that authenticates the user

- اگر docstring نداشت:
  - از نام فایل و نام تابع برای تولید instruction استفاده شد

- `output` → بدنه کامل تابع یا کلاس

### فیلتر طول

- توابع: بین ۵۰ تا ۵۰۰۰ کاراکتر  
- کلاس‌ها: بین ۱۰۰ تا ۸۰۰۰ کاراکتر  

---

## 2️⃣ استخراج از Commits

دو نوع نمونه تولید شد:

### نوع اول: پیام → تغییرات

- `instruction` = پیام commit  
- `output` = diff تغییرات  

مثال:
> Implement the following change: Fix authentication bug

---

### نوع دوم: تغییرات → پیام

- `input` = diff  
- `output` = پیام commit مناسب  

این نوع نمونه برای آموزش تولید commit message بسیار مفید است.

---

## 3️⃣ استخراج از README

- بخش‌های README بر اساس هدرهای Markdown جدا شدند
- هر بخش یک نمونه آموزشی شد
- یک نمونه ویژه برای تولید کل README ساخته شد

---

# ❓ دیتاست دوم: QA (سوال و جواب)

این دیتاست از **Issues** استخراج شد و شامل سه روش تولید است:

---

## 1️⃣ سوال و جواب عمومی

- `instruction` = عنوان + بدنه Issue  
- `output` = کامنت‌ها  

اگر چند کامنت وجود داشت:

- یک نمونه با **اولین کامنت (بهترین پاسخ سریع)**  
- یک نمونه با **ترکیب چند کامنت (پاسخ جامع)**  

اگر Issue شامل بلاک کد بود:
- یک نمونه تکنیکال اضافی تولید شد

---

## 2️⃣ Issues حل‌شده

از Issues بسته‌شده نمونه‌هایی با الگوی زیر ساخته شد:

> How was this issue resolved?

- `output` = آخرین کامنت (راه‌حل نهایی)

---

## 3️⃣ سوالات How-to

Issues که عنوانشان شامل کلمات زیر بود فیلتر شدند:

- how  
- what  
- why  
- is it possible  
- و موارد مشابه  

از آن‌ها نمونه‌های آموزشی How-to ساخته شد.

---

## 🔁 حذف داده‌های تکراری

نمونه‌های تکراری بر اساس **۱۰۰ کاراکتر اول instruction** حذف شدند تا از تکرار زیاد جلوگیری شود.

---

# 🐞 دیتاست سوم: Debug (دیباگ و Code Review)

این دیتاست از دو منبع ساخته شد:

---

## 1️⃣ استخراج از Pull Requests

سه نوع نمونه تولید شد:

### 🔧 Bug Fix

PRهایی با کلمات:

- fix  
- bug  
- patch  
- error  

دو نمونه از هرکدام ساخته شد:

- توضیح باگ → diff راه‌حل  
- diff → توضیح باگ  

---

### 👀 Code Review

- `input` = diff تغییرات  
- `output` = review comments  

---

### 📍 Inline Review

از reviewهای خط‌به‌خط:

- `diff_hunk` + نظر reviewer  
- تبدیل به نمونه‌های inline review

---

## 2️⃣ استخراج از Bug Issues

Issues مرتبط با خطا فیلتر شدند.

### حالت‌های تولید نمونه:

- اگر شامل traceback یا error block بود:
  > Debug this error

- اگر بسته شده بود:
  > How to fix this issue?

- اگر traceback وجود داشت:
  > Analyze this error traceback

---

# 📦 ادغام و خروجی نهایی

پس از تولید هر سه دیتاست:

### فایل‌ها:

- `instruction_dataset.jsonl`
- `qa_dataset.jsonl`
- `debug_dataset.jsonl`
- `full_dataset.jsonl` (همراه با metadata)
- `alpaca_format_dataset.jsonl` (فقط سه فیلد اصلی)

---

## ⚠️ نکته بسیار مهم

فقط فایل زیر باید برای Fine-tuning استفاده شود:
alpaca_format_dataset.jsonl

اگر هر پنج فایل با هم ZIP شوند:

- هر نمونه سه بار تکرار می‌شود
- باعث **Overfitting** مدل خواهد شد

---

# ✅ نتیجه

- داده‌ها از Gitea API خوانده شدند  
- سه دیتاست جداگانه ساخته شد  
- یک فایل ادغام‌شده کامل تولید شد  
- تمام نمونه‌ها دارای metadata منبع و نوع هستند  
- فایل نهایی آماده آپلود در Google Colab برای Fine-tuning در گام بعدی است  

این گام، دیتاستی ساخت‌یافته، قابل‌تحلیل و مناسب آموزش مدل زبانی تخصصی حوزه کدنویسی ایجاد کرد.