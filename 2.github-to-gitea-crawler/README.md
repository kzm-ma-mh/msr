# 🕷️ گام ۲: کرول هوشمند از GitHub و انتقال به Gitea

## 🎯 هدف

در این گام، یک کرولر هوشمند پیاده‌سازی شد که پروژه‌های متن‌باز را از GitHub دریافت کرده و به Gitea محلی منتقل می‌کند.

هدف اصلی این مرحله، جمع‌آوری داده‌های غنی شامل موارد زیر برای ساخت دیتاست آموزشی تخصصی بود:

- سورس کد
- Issues
- Pull Requests
- Commits

---

## 📦 پروژه انتخاب‌شده

پروژه **FastAPI** (`fastapi/fastapi`) به دلیل داشتن Issues و Pull Requests غنی و متنوع انتخاب شد.

ویژگی‌های مهم این پروژه:

- بیش از ۸۰٬۰۰۰ ⭐ در GitHub
- مشارکت فعال جامعه توسعه‌دهندگان
- بحث‌های فنی عمیق در Issues و PRها
- ساختار کد تمیز و مستندات قوی

این ویژگی‌ها آن را به گزینه‌ای مناسب برای ساخت دیتاست آموزشی در حوزه مهندسی نرم‌افزار و Code Review تبدیل می‌کند.

---

## 📊 داده‌های کرول‌شده

در این مرحله از **GitHub API v3** استفاده شد و داده‌های زیر دریافت گردید:

### 🧩 سورس کد

- ۲۰۰ فایل با پسوندهای مجاز مانند `.py`، `.md` و `.yaml`
- فیلتر فایل‌های بزرگ‌تر از ۱۰۰ کیلوبایت
- حذف پوشه‌های غیرضروری مانند `node_modules` و `__pycache__`

### 🐛 Issues

- ۱۱۸ Issue
- شامل عنوان، بدنه، وضعیت (Open / Closed)، لیبل‌ها و تمام کامنت‌ها
- مرتب‌سازی بر اساس تعداد کامنت برای دریافت بحث‌های باارزش‌تر

### 🔀 Pull Requests

- ۵۰۰ PR
- شامل عنوان، توضیحات کامل، وضعیت Merge
- فایل‌های تغییر یافته با diff کامل
- Review comments خط‌به‌خط

این داده‌ها برای ساخت دیتاست‌های Debug، Code Review و تحلیل تغییرات کد بسیار ارزشمند هستند.

### 🧾 Commits

- ۲۰۰ Commit
- شامل پیام Commit، نام نویسنده، تاریخ و Patch تغییرات هر فایل

---

## ⚙️ مدیریت محدودیت GitHub API

GitHub API با استفاده از توکن احراز هویت اجازه **۵۰۰۰ درخواست در ساعت** را می‌دهد.

کرولر:

- هدر `X-RateLimit-Remaining` را بررسی می‌کند
- در صورت رسیدن به محدودیت، تا زمان بازنشانی صبر می‌کند
- سپس فرآیند را به‌صورت خودکار ادامه می‌دهد
- تمام درخواست‌ها به‌صورت صفحه‌بندی شده (Pagination) ارسال می‌شوند

---

## 🔄 انتقال به Gitea

### انتقال سورس کد و JSON خام

انتقال فایل‌ها با استفاده از Git مستقیم انجام شد (`clone / commit / push`).

دلیل این انتخاب، خطای `422 (SHA conflict)` در Gitea API بود. در صورتی که فایل از قبل وجود داشت، API به SHA نسخه قبلی نیاز داشت که باعث خطا می‌شد. با استفاده از Git مستقیم این مشکل برطرف شد.

### انتقال Issues

Issues از طریق Gitea REST API ساخته شدند:

- ایجاد Issue با عنوان و بدنه
- افزودن متادیتا
- افزودن تمام کامنت‌ها
- بستن Issue در صورت بسته بودن در GitHub

### انتقال Pull Requests

به دلیل نیاز Gitea به Branch جداگانه برای ساخت PR واقعی، Pull Requestها به‌صورت Issue ذخیره شدند.

برای تمایز، عنوان PRها با پیشوند زیر ثبت شدند:


بدنه هر PR شامل توضیحات کامل، فایل‌های تغییر یافته با diff و Review comments است.

---

## 💾 Backup محلی

قبل از Push به Gitea، تمام داده‌های کرول‌شده در فایل زیر ذخیره شد:

این فایل حدود ۵۰ مگابایت حجم دارد و شامل سورس کد، Issues، PRها و Commits است.

این Backup در مراحل بعدی (ساخت دیتاست و ایندکس‌گذاری در ChromaDB) بدون نیاز به دسترسی مجدد به GitHub یا Gitea استفاده خواهد شد.

---

## ⏱ زمان اجرا

| مرحله | زمان تقریبی |
|------|-------------|
| کرول از GitHub | ۱۵ تا ۲۰ دقیقه |
| Push فایل‌ها با Git | ۱ تا ۲ دقیقه |
| ساخت ۱۱۸ Issue | ۳ تا ۵ دقیقه |
| ساخت ۵۰۰ PR | ۱۰ تا ۱۵ دقیقه |
| **کل فرآیند** | **۳۰ تا ۴۰ دقیقه** |

---

## ✅ نتیجه نهایی

در پایان این گام:

- پروژه FastAPI به‌طور کامل در Gitea محلی قابل مشاهده است
- مجموعاً ۶۱۸ Issue ثبت شده است:
  - ۱۱۸ Issue واقعی
  - ۵۰۰ PR ذخیره‌شده به‌صورت Issue
- تمام کامنت‌ها و متادیتا منتقل شده‌اند
- داده‌های خام JSON در پوشه `_crawled_data` ریپازیتوری ذخیره شده‌اند
- فایل Backup محلی برای مراحل بعدی آماده است

آدرس دسترسی محلی:

---

## 🏁 جمع‌بندی

این گام یک زیرساخت داده‌ای کامل برای مراحل بعدی (ساخت دیتاست آموزشی و ایندکس‌برداری معنایی) فراهم کرد و وابستگی به GitHub در مراحل بعدی را به‌طور کامل حذف نمود.